{
  "name": "@workspace/media",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "truncate": "bun run src/db/truncate.ts"
  },
  "dependencies": {
    "@workspace/types": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.3.0",
    "sharp": "^0.33.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@workspace/config": "workspace:*"
  }
}
